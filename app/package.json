{
  "name": "app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "build": "bun build src/index.ts --target bun --outdir /private/tmp/garage-api-build",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.19",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  }
}
